export enum MessageType {
  TEXT = 'text',
  AUDIO = 'audio',
  FILE = 'file',
}
export enum AttachmentType {
  IMAGE = 'image',
  TEXT = 'text',
  WORD = 'word',
  PDF = 'pdf',
  EXCEL = 'excel',
  PPT = 'ppt',
  ZIP = 'zip',
  MARKDOWN = 'markdown',
}
